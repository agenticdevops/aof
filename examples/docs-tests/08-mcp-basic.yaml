# From docs/user/MCP_CONFIGURATION.md - Basic MCP Configuration
name: my-agent
model: google:gemini-2.5-flash

instructions: |
  You are a helpful assistant with access to various tools.

mcp_servers:
  - name: filesystem
    transport: stdio
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-filesystem"
      - "/tmp"

max_iterations: 10
