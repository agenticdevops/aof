# From docs/user/CLI_REFERENCE.md - Kubernetes-style format
apiVersion: aof.dev/v1
kind: Agent
metadata:
  name: my-agent
  labels:
    environment: production
spec:
  model: google:gemini-2.5-flash
  instructions: You are a helpful assistant.
  tools:
    - shell
    - read_file
